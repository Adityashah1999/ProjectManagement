CREATE TABLE  "CART" 
   (	"CART_ID" NUMBER(30,0), 
	"CART_STATUS" NUMBER(30,0), 
	"USER_ID" NUMBER(30,0), 
	 CONSTRAINT "PK_CART" PRIMARY KEY ("CART_ID") ENABLE
   )
/
ALTER TABLE  "CART" ADD FOREIGN KEY ("USER_ID")
	  REFERENCES  "USERA" ("USER_ID") ENABLE
/