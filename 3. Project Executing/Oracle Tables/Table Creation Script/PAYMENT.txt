CREATE TABLE  "PAYMENT" 
   (	"PAYMENT_ID" NUMBER(30,0), 
	"PAYMENT_STATUS" VARCHAR2(500) NOT NULL ENABLE, 
	"PAYMENT_TYPE" VARCHAR2(500) NOT NULL ENABLE, 
	"PAYMENT_DATE" VARCHAR2(500), 
	"USER_ID" NUMBER(30,0), 
	 CONSTRAINT "PK_PAYMENT" PRIMARY KEY ("PAYMENT_ID") ENABLE
   )
/
ALTER TABLE  "PAYMENT" ADD FOREIGN KEY ("USER_ID")
	  REFERENCES  "USERA" ("USER_ID") ENABLE
/