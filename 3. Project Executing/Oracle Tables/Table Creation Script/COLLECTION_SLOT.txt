CREATE TABLE  "COLLECTION_SLOT" 
   (	"SLOT_ID" NUMBER, 
	"SLOT_TIME" VARCHAR2(500), 
	"SLOT_DATE" VARCHAR2(500), 
	"SLOT_STATUS" VARCHAR2(500), 
	"USER_ID" NUMBER, 
	 CONSTRAINT "PK_COLLECTION_SLOT" PRIMARY KEY ("SLOT_ID") ENABLE
   )
/
ALTER TABLE  "COLLECTION_SLOT" ADD FOREIGN KEY ("USER_ID")
	  REFERENCES  "USERA" ("USER_ID") ENABLE
/