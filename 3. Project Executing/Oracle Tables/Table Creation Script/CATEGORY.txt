CREATE TABLE  "CATEGORY" 
   (	"CATEGORY_ID" NUMBER(30,0), 
	"CATEGORY_NAME" VARCHAR2(500) NOT NULL ENABLE, 
	"PRODUCTS_QUANTITY" NUMBER(30,0), 
	"CATEGORY_TYPE" VARCHAR2(500), 
	 CONSTRAINT "PK_CATEGORY" PRIMARY KEY ("CATEGORY_ID") ENABLE
   )
/